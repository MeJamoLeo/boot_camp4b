#include <bits/stdc++.h>
using namespace std;
// oj t -c "./a.out" -d ./tests

int main() {
    int N;
    cin >> N;
    vector<int> students(N);

    for (int i = 0; i < N; i++) {
	cin >> students.at(i);
    }

    cout <<  << endl;
    return 0;
}
